# Surgeonfish Visual Phenomics and Phylogenetic Inference

## Version

**1.3.3** — see [CLAUDE.md](CLAUDE.md) for the versioning scheme (major = phase completion, minor = a step within a phase, patch = a bug fix).

**Changelog**
- **1.3.3** — The full pipeline ran and generated a real review page, but its flagged-image thumbnails came up blank/broken. Cause: `review.py`'s image links are relative (`../data/raw_images/...`), which only resolve when something serves the whole project tree together — opening the `.html` file via Drive's own web preview (clicking it on drive.google.com) sandboxes the file and doesn't serve its sibling folders, so every image link 404s. Fixed `notebooks/Phase1_Fish_Extraction.ipynb`'s review-page step to start a local `http.server` rooted at the project folder and open it through Colab's authenticated port proxy (`google.colab.output.serve_kernel_port_as_window`), which serves the real directory tree so the relative links resolve correctly — no dependency on whether the user has Drive for Desktop synced locally. Kept a note that opening straight from a locally-synced Drive folder also works fine (only Drive's *web* preview breaks it, not Drive in general). No pipeline code changed.
- **1.3.2** — Another real bug from the same Colab smoke-test run, one step further than v1.3.1 (Grounding DINO detection now succeeded; SAM 2 segmentation crashed next): `segmenter.py`'s `_ensure_loaded()` never told `SAM2ImagePredictor.from_pretrained()` which device to use, and the `sam2` package's own `build_sam2()` defaults to `device="cuda"` unconditionally rather than auto-detecting — so on a runtime without working CUDA it crashed with `AssertionError: Torch not compiled with CUDA enabled` instead of falling back to CPU. `detector.py` already had the correct pattern (`"cuda" if torch.cuda.is_available() else "cpu"`); `segmenter.py` now matches it. Also added an `nvidia-smi` check as the very first cell in `notebooks/Phase1_Fish_Extraction.ipynb`, since `Runtime -> Change runtime type -> GPU` only *requests* a GPU — it doesn't guarantee Colab attaches one — and catching that before two model downloads is cheaper than catching it after.
- **1.3.1** — Real bug, caught by actually running `notebooks/Phase1_Fish_Extraction.ipynb`'s smoke-test cell on Colab (the first real execution of `fish_extractor` against a live Hugging Face model, something no unit test exercises — `detector.py`/`segmenter.py` are fully mocked out in the test suite by design, see their module docstrings): `GroundingDinoProcessor.post_process_grounded_object_detection()` renamed its box-confidence keyword from `box_threshold` to `threshold` in `transformers` 4.51, then removed the old name entirely in 4.55 — Colab installed a version past that removal, so `detector.py`'s call raised `TypeError: ... got an unexpected keyword argument 'box_threshold'`. Fixed the call site to use `threshold=`, bumped the `transformers` pin in `pyproject.toml` to `>=4.51` (the version where the new name first exists) with a comment explaining why, and updated the notebook's install line to match. `DetectionConfig.box_threshold` (this project's own config field name) is unchanged — only the keyword passed to the library call changed.
- **1.3.0** — Added [`notebooks/Phase1_Fish_Extraction.ipynb`](notebooks/Phase1_Fish_Extraction.ipynb) and [`notebooks/Phase2_Pattern_Extraction.ipynb`](notebooks/Phase2_Pattern_Extraction.ipynb), the Colab tooling needed to actually run the already-built Phase 1/2 code — a discrete new step, not a bug fix, and not the phases' completion (that's when they've actually been run against the real dataset). Both notebooks clone the project **directly onto Google Drive** rather than Colab's ephemeral local disk — the key design choice, since it's what keeps the resumable per-image state, the extracted images, and the review page's image links (which use paths relative to wherever the HTML file sits, per `review.py`) valid across a Colab disconnect/restart, and openable from Drive afterwards rather than only for one runtime's lifetime. Phase 1's notebook installs only the GPU-specific extras on top of Colab's preinstalled `torch`/CUDA (rather than the project's full `vision` extra, which would risk reinstalling and mismatching those), includes an optional one-species smoke test before committing to the full 64-species run, and wires up the review-page-generate/apply-feedback loop. Phase 2's notebook needs no GPU (`pattern_extractor` is pure NumPy/SciPy/Pillow) and reuses the same Drive-resident project directory Phase 1 wrote `data/extracted_fish/` into. No pipeline code changed.
- **1.2.5** — A fifth external audit round, checked directly against `data/raw_images/*.zip` contents (not just the aggregate `raw_images_state.json` counts) and against the `geomorph` manual's `compare.physignal.z` worked example: (1) **Real finding, confirmed by opening the file**: `data/raw_images/Naso/Naso_maculatus.zip` contains exactly one file, `000_reference.jpeg` — GBIF sourcing found zero acceptable field photos for this species, so its single image *is* the curated seed photo, not a separate observation. That means "exclude `is_reference` rows from the aggregate" and "keep all 50 species in the Kmult set" are **mutually exclusive for this species specifically**, not two independent open decisions as previously described — excluding the reference photo would drop the analysis set to 49, not just shrink one species' aggregate. Each Phase 3/4 sensitivity run must now assert its own expected species count (49/46/49) instead of reusing the primary run's `assert matched == 50`. (2) **Preregistered, no longer deferred**: the Kmult lambda-optimization method and `PAC.no` are now pinned to `lambda = "front"` with one shared `PAC.no` across all three pattern dimensions, matching the `geomorph` manual's own `compare.physignal.z` worked example exactly (`lambda = "front", PAC.no = 7` for both compared traits). `"front"` was chosen specifically over `"burn"` — which maximizes Z, the very statistic being compared across dimensions, a data-dredging-shaped risk this project has been burned by before — and over `"all"`, which tends toward the already-guarded `NaN`-Z failure case. `"front"`'s own documented cost (biases lambda toward 1) is stated as a real limitation, not hidden. (3) Seed handling stated explicitly: `physignal.z`'s deterministic `seed = NULL` default will be used, matching this project's other reproducibility measures. No pipeline code changed this round — Phase 3/4 statistical code still doesn't exist.
- **1.2.4** — A fourth external audit round, checked directly against the real `geomorph` reference manual (fetched the CRAN PDF, converted to text, read the actual `physignal`/`physignal.z`/`physignal.eigen`/`compare.physignal.z` documentation) and against this project's own `data/raw_images_state.json`, rather than trusted: (1) **Real design mistake, fixed**: the plan had `physignal()` as the primary Kmult test and `physignal.z()` as a separate tool for cross-dimension comparison — but `physignal()`'s own manual entry says it "no longer reports an effect size" as of geomorph 4.0.2 and defers to `physignal.z`, whose output is a strict superset of `physignal()`'s (K, lambda, and the effect size Z together, under one optimized model). Running both would produce two K values computed under different tree scalings with no principled way to reconcile a disagreement. `physignal.z` is now the single primary per-dimension test; `physignal()` is kept only as an optional, explicitly-labeled unit-tree sensitivity check. The lambda-optimization method and `PAC.no` will be declared in advance, not chosen post hoc. (2) **My own error, found worse than the audit realized**: v1.2.3 stated the sparsest of the 9 short-of-target species had "as few as 4" images (unverified — I estimated it rather than checking); the audit correctly flagged this as inconsistent with the "~2.5x" figure in the same sentence, but its own recomputation (6.2x) inherited the same unverified number. Checked `data/raw_images_state.json` directly: the true minimum is **1 image**, not 4 (*Acanthurus albimento*, *A. chronixis*, *Naso maculatus*, *Naso tergus*). Narrowed further against the actual 50-species Kmult set (`species_coverage.csv`): only 4 of those 9 short species are even in the 50 — *Naso maculatus* (1 image), *N. tuberosus* (13), *Zebrasoma gemmatum* (20), *N. fageni* (22); the other 5 are already excluded by the phylogeny restriction. Real worst-case spread within the Kmult set is **25x**, not the audit's estimated 6.2x, driven entirely by *N. maculatus*'s single image — added a sensitivity check dropping that species specifically, on top of the general sparse-species check. (3) Rewrote the tip-count check as a range assertion (`10,000 < ntip < 15,000`, both candidate tree sizes named in a comment) instead of an exact match that would break on any release differing by one tip. (4) Added a note on `physignal.eigen` (a different question — signal *within* one matrix, not *across* our three) and reframed the CLR/ILR/standardization/condition-number requirements as admissibility requirements for applying a shape-signal method to non-shape data, not routine hygiene. No pipeline code changed this round — Phase 3/4 statistical code still doesn't exist, so this is a plan and documentation correction.
- **1.2.3** — A third external audit round, mostly the auditor's own self-corrections to its v1.2.2 findings — each independently re-verified against NCBI Taxonomy directly (all six genera re-queried, every returned name hand-classified) before acting, rather than trusted: (1) **Real bug, fixed**: prior per-genus/family taxon-sampling percentages counted NCBI interspecific hybrids and `sp.`/`aff.`/`cf.`/BOLD placeholder records as if they were extant species, understating coverage. Corrected and saved to `data/genus_species_counts.json`: family **77%** (not 60%), *Ctenochaetus* **9/9 = 100%** (was shown as 69% — the single biggest change, flipping a genus from "coverage gap" to "fully sampled"), *Naso* 70%, *Acanthurus* 79%, *Zebrasoma* 86%, *Prionurus* 43% (unaffected, already clean — now the only genus clearly below the family mean). (2) **Favorable finding, confirmed**: the two species Sorenson et al. (2013) actually name in the *Acanthurus*/*Ctenochaetus* paraphyly (*A. nubilus*, *A. pyroferus*) are both present in the 50-species phylogeny-matched set, so that prediction is testable as named — with the caveat that *Acanthurus* coverage itself drops to 54% (21/39) after the phylogeny restriction, the lowest of any genus. (3) **Real code change**: `pattern_extractor`'s per-image output CSV now has an `is_reference` boolean column marking the curated seed photo (`000_reference.jpg`) versus GBIF field images, so a future per-species feature-aggregation step (Phase 3) can deliberately include or exclude it rather than by accident — 107 tests passing (one new), ruff clean. (4) **Implementation-risk finding, changed the plan**: hand-porting `physignal.z`/`compare.physignal.z` to Python is high-risk (PACA projection, branch-scaling likelihood optimization, RRPP permutation, Box-Cox effect size — not simple formulas, and this project has already shipped one silently-wrong metric before); Phase 4's plan now prefers running those two statistics in R (`rpy2` or a documented handoff step) over porting them, and requires a numerical-equivalence test against `geomorph`'s own `plethspecies` dataset if ported anyway — the same equivalence-check gap was flagged for the already-built *patternize* reimplementation and is now a tracked follow-up. (5) Added explicit, not-yet-built Phase 3/4 requirements: a per-species feature-aggregation rule (circular-hue handling, per-species n recorded, sparse-species sensitivity check), a tree tip-count assertion (~11,638) and prune-before-cophenetic ordering, and a feature-row-order-vs-tip-label-order assertion. None of Phase 3/4's statistical code exists yet, so beyond the `is_reference` column this round changed plans and documentation, not the pipeline's scientific output.
- **1.2.2** — A second external audit round re-checked the v1.2.1 corrections (all confirmed exact against Crossref/NCBI) and found a real bug plus three scope-accuracy gaps, all independently re-verified before acting: (1) **Bug fix**: species-matching against the Fish Tree of Life used exact string matching only, missing a confirmed synonym (*Zebrasoma veliferum* = tree tip *Zebrasoma velifer*, same NCBI tax ID). Corrected count: **50/64 species (78.1%)**, not 49/64 — re-verified directly against the tree file with bidirectional synonym resolution; the other 17 of 18 "unused" tree tips turned out to be genuinely different Acanthuridae species not in this study, not recoverable matches, so the audit's implied scale of the bug was larger than what was actually fixable. `data/phylogeny/acanthuridae_synonyms.json` (the NCBI synonym table used for this check) and a tree-file checksum are now saved for reproducibility. (2) **Overreaching claim, corrected**: the Research Question implied the test could distinguish "shared ancestry" from "shared habitat/ecology" as the reason for pattern similarity — Kmult/Mantel can't make that distinction on their own, since ecology is itself phylogenetically structured; the claim is now scoped to what these statistics actually measure (pattern-phylogeny covariance), with the ecological-covariate/PGLS extension needed for the stronger claim noted as out of scope for now. (3) Added Kmult's feature-matrix assumptions (compositional-data handling, standardization, dimensionality vs. n=50) and `physignal.z`/`compare.physignal.z` (Collyer, Baken & Adams 2022) for cross-dimension effect-size comparison to the Phase 4 plan — not yet built, so this changed requirements, not code. (4) Extended the multiple-comparisons-correction requirement to the primary Kmult tests, not just the secondary Mantel tests. Also confirmed via NCBI Nucleotide that most of the still-unmatched 14 species have GenBank sequence data not incorporated into this particular Fish Tree of Life release, a more accurate framing than "no genetic data exists."
- **1.2.1** — Corrections from an external README audit, each independently re-verified against a primary source before acting on it: (1) the patternize citation had two fabricated author names ("Planas, S.", "Martin, S. H.") that don't exist in the real 7-author list — fixed everywhere it appeared, confirmed against Crossref. (2) The "nine molecular loci" phrase was misattributed to the Fish Tree of Life; it actually describes a different paper (Sorenson et al. 2013) — fixed, and that paper is now properly cited as the alternative phylogeny source considered. (3) The 1.0.0 entry's image counts (~1,850/~490) didn't reconcile against the actual dataset; replaced with the real numbers read directly from `data/raw_images_state.json` (2,329 evaluated, 1,460 accepted, 869 rejected — internally consistent with 55×25 + the 9 short species' counts). (4) **Blocking methodological finding, resolved**: the Fish Tree of Life's complete phylogeny places species lacking genetic data via stochastic polytomy resolution, which its own documentation says shouldn't be used for trait-evolution analyses (Rabosky 2015) — checked directly (`data/phylogeny/`, robots.txt-permitted download of the genetic-data-only tree): **49/64 study species (76.6%) have real genetic-data placement**; Phase 4 will restrict the phylogenetic side of the analysis to those 49 rather than use imputed placements for the other 15. (5) **Second blocking finding, resolved**: Harmon & Glor's (2010) phylogenetic-permutation remedy, previously cited to justify this project's Mantel tests, specifically addresses *three-way* Mantel tests — this project's per-dimension tests are two-way, a case the same paper says is better served by a K-statistic. Kmult (Adams 2014) is now the planned primary test per dimension, with the phylogenetically-permuted Mantel tests kept as a secondary check (see Planned Approach). (6) Documented taxon-sampling unevenness across genera (*Prionurus* 43%, *Naso* 58% vs. 60% family mean) and the *Ctenochaetus*/*Acanthurus* paraphyly's taxonomic-convention implications as explicit limitations (Species section). None of Phase 4's code exists yet, so this round changed plans and documentation, not implementation.
- **1.2.0** — Phase 1 (`src/fish_extractor/`) code-complete and unit-tested: Grounded SAM 2 zero-shot fish detection/segmentation, the accept/flag QA gate, resumable per-image state, a human-review page for flagged images, and a `--run`-gated CLI (mirrors `dataset_builder`'s `--scrape-web` pattern — no model loads unless explicitly asked). Not yet run against the real dataset — that needs Colab's GPU and is the actual Phase 1 completion event. Also added a working Phase 2 prototype (`src/pattern_extractor/`): coloring/stripe/spot feature extraction from `fish_extractor`'s output, built around a Python reimplementation of *patternize*'s (Van Belleghem et al. 2018) reference-initialized k-means colour-clustering method — see Planned Approach below for the full citation and how this differs from the original R package. Two rounds of independent code review caught and fixed real bugs before either lands: a missing file extension that would have silently skipped `.webp` images, a non-deterministic clustering seed, a stripe-periodicity check that was blind to same-luminance/different-hue stripes, an unguarded crash path on degenerate (near-solid-colour or empty-mask) images, and a dead config field. 106 tests passing.
- **1.1.0** — Literature review completed for the candidate-genetics/methodology groundwork behind the pattern-extraction and phylogenetic-comparison design (see [BACKGROUND.md](BACKGROUND.md)): 56 NCBI-validated candidate genes across the coloring/stripes/spots dimensions (`data/genes/`), sourced from five papers on zebrafish, spotted scat, medaka, and mammalian pigmentation genetics — developmental corroboration for treating the three pattern dimensions as separable, not just an engineering convenience. Also surfaced a Mantel-test methodology correction (Harmon & Glor 2010): phylogenetic permutation, not naive permutation, and a null result must be reported as inconclusive rather than as evidence of no association — both folded into the Phase 4 plan. Separately, queried NCBI for genome-assembly availability across the 64 species (`data/genome_assemblies/`): 16/64 (25%) have a public assembly. Metadata only, no sequence downloaded — this is groundwork for a later, contingent phase, not new pipeline code.
- **1.0.0** — Phase 0 (data collection) complete. 2,329 candidate images evaluated from GBIF across all 64 species over 9 human-review rounds; 1,460 accepted into the final dataset (869 rejected and, where possible, backfilled). 55/64 species reached the 25-image target (55×25 + the 9 short species' actual counts = 1,460, reconciled directly against `data/raw_images_state.json`); the remaining 9 are genuinely limited by GBIF's available licensed photos, not a pipeline gap. Along the way: fixed a Windows encoding crash, a filename-numbering bug (with a cleanup pass over the existing dataset), and confirmed automated close-up/background filtering isn't reliable enough to replace human review.

This section will be trimmed down once the project is finished — for now it's tracking everything as it happens.

---

## Research Question

**Is visual pattern similarity among surgeonfish (family Acanthuridae) genetically associated with evolutionary relatedness — or does it arise independently of shared ancestry?**

Concretely: for a set of Acanthuridae species with a known molecular phylogeny, does quantified visual similarity (color pattern, texture) between species pairs correlate with their phylogenetic distance? A significant result (tested primarily via Kmult, a multivariate phylogenetic-signal statistic, with a distance-based Mantel test as a secondary check — see Planned Approach) is evidence that visual pattern covaries with phylogenetic relatedness — closely related species tend to look more alike than distantly related ones do. A null result means visual pattern is not explained by relatedness at the level these methods can detect, and any resemblance between species is more likely convergent.

**What this test cannot, on its own, distinguish**: pattern covarying with phylogeny is consistent with pattern being heritable, but it's equally consistent with pattern tracking an ecological variable (diet, depth, reef zone) that is itself phylogenetically conserved — closely related surgeonfishes tend to share ecology, not just genes. Kmult/Mantel alone cannot separate "inherited" from "conserved-ecology-mediated" as the reason for a significant result. Testing that distinction specifically would require ecological covariates (e.g. from FishBase) and a phylogenetic-regression approach (`procD.pgls`) — not yet in scope, noted here so the claim this project can support isn't overstated.

This is a correlational test, not a claim of direct causation: both statistics establish association between visual pattern and phylogeny. Neither, on its own, identifies the underlying genetic or developmental mechanism, or separates heritability from conserved ecology.

---

## Status

This project is being rebuilt from scratch after an earlier pipeline run produced unreliable results (a broken evaluation metric, a misreported ROC-AUC, a silently-dropped test image, and a phylogenetic-signal result that came from data-dredged feature selection rather than a preregistered test). Rebuilding proceeds one phase at a time (see [CLAUDE.md](CLAUDE.md)), each verified against its own output before moving to the next — see the changelog above for what's done so far.

Nothing in this README describes final scientific results yet — that section gets written last, once every number in it traces back to a real output file.

---

## Species

64 species across all six extant Acanthuridae genera, sourced from lateral reference photographs:

| Genus | Species count |
|---|---|
| *Acanthurus* | 31 |
| *Ctenochaetus* | 9 |
| *Naso* | 14 |
| *Paracanthurus* | 1 |
| *Prionurus* | 3 |
| *Zebrasoma* | 6 |
| **Total** | **64** |

**Taxon sampling is uneven across genera** relative to each genus's full species count in NCBI Taxonomy, **counting only valid binomial species** (interspecific hybrids and provisional `sp.`/`aff.`/`cf.`/BOLD placeholder records excluded — see `data/genus_species_counts.json` for the per-genus exclusion list, independently queried and hand-classified for this project): *Prionurus* 3/7 (43%), *Naso* 14/20 (70%), *Acanthurus* 31/39 (79%), *Zebrasoma* 6/7 (86%), *Ctenochaetus* 9/9 (100%), *Paracanthurus* 1/1 (100%) — against a family mean of **64/83 (77%)**. (An earlier draft of this table used raw NCBI Taxonomy species-rank counts, which include those hybrids/placeholders as if they were extant species — that inflated every genus's denominator and understated coverage; e.g. *Ctenochaetus* looked 69% sampled and was actually fully sampled. NCBI Taxonomy is still a sequence-database index, not a taxonomic authority for fishes — Eschmeyer's Catalog of Fishes or FishBase would be a better final source if this table needs to hold up outside this repo.) *Prionurus* at 43% is now the only genus clearly below the family mean; *Naso* — one of Acanthuridae's two deep clades (Nasinae, crown age ~17 Ma per Sorenson et al. 2013 below) — is second-lowest at 70%, still a real concern since sparse, non-random sampling there disproportionately shapes the large phylogenetic distances that most influence a Mantel/Kmult result. Missing species were dropped for image availability (Phase 0), not by design — this is a real limitation, not a curated subsample.

A key biological complication: *Acanthurus* is paraphyletic with respect to *Ctenochaetus* in the molecular phylogeny — Sorenson et al. (2013) recover the paraphyly specifically as *Ctenochaetus* nested with two named *Acanthurus* species, *A. nubilus* and *A. pyroferus*. Whether that paraphyly is also reflected in visual similarity is one specific, checkable prediction of the broader research question above, and both named species are confirmed present in the 50-species phylogeny-matched set below (`data/phylogeny/species_coverage.csv`), so the prediction is testable as named, not just as a genus-level proxy. Sorenson et al. recommend dissolving *Ctenochaetus* into *Acanthurus* on this basis; this project keeps them as separate genus labels (standard reference-database convention) since genus is a descriptive grouping here, not part of the statistical test itself, which operates at the species-tip level. One caveat worth flagging here rather than only in the phylogeny paragraph below: restricting to genetically-sampled species drops *Acanthurus* coverage from 79% (31/39) to **54% (21/39)** — the lowest of any genus post-restriction, and the genus this specific prediction most depends on being well sampled. The two species the prediction actually names are both present, but the wider *Acanthurus* context around them is thinner than the pre-restriction number suggests.

The molecular phylogeny used as the evolutionary-relatedness reference comes from the Fish Tree of Life (fishtreeoflife.org, Rabosky et al. 2018, *Nature* 559, 392–395), specifically its **genetic-data-only tree** (not the "complete" tree, which places species lacking genetic data via stochastic polytomy resolution — a taxonomy-conditioned random draw, not phylogenetic signal; see `data/phylogeny/README.md` for the full reasoning, source, and a checksum for reproducibility). **50 of the 64 study species (78.1%) have real genetic-data placement** in that tree (49 by exact name match, 1 — *Zebrasoma veliferum* — via a confirmed taxonomic synonym, cross-checked only against NCBI's synonym table so far — see `data/phylogeny/README.md` for the standing caveat on that); the analysis is restricted to those 50. The 14 still unmatched are concentrated in *Acanthurus* (10 of 14; already the best-sampled genus pre-restriction) rather than compounding the *Naso*/*Prionurus* sampling gap above — most have GenBank sequence data that simply wasn't part of this tree's fitted supermatrix, not an absence of genetic data altogether; see `data/phylogeny/species_coverage.csv` and `README.md` for the full per-species breakdown and reasoning. ("Nine molecular loci" describes a different, Acanthuridae-specific timetree — Sorenson, L., Santini, F., Carnevale, G., & Alfaro, M. E. (2013). A multi-locus timetree of surgeonfishes (Acanthuridae, Percomorpha), with revised family taxonomy. *Molecular Phylogenetics and Evolution*, 68(1), 150–160. https://doi.org/10.1016/j.ympev.2013.03.014 — considered as the phylogeny source and not currently used; see `data/phylogeny/README.md`.)

---

## Data Sourcing & Compliance

Images are collected programmatically via `src/dataset_builder/` (see its package docstring for full detail), not hand-collected or scraped from arbitrary web search results. In summary:

- **Source**: [GBIF](https://www.gbif.org)'s public REST API (`occurrence/search`), which aggregates research-grade, Creative-Commons-licensed photos from iNaturalist and other providers. GBIF's own `robots.txt` permits this; iNaturalist's own API host disallows generic automated query-string access, so this pipeline goes through GBIF rather than around that boundary.
- **robots.txt is enforced at runtime** before every request this pipeline makes — including each image download, which can land on a different host than the API itself — not just checked once by hand.
- **License allowlist**: only CC0/public-domain and CC-BY/BY-SA/BY-NC/BY-NC-SA images are accepted; no-derivatives (ND) licenses are excluded since this pipeline produces derivative works (masks, crops, figures). License and attribution are logged per image to `reports/image_sourcing_log.csv`.
- **Rate-limited and self-identifying**: one request/second by default, with a descriptive `User-Agent` and contact address, and backoff-retry (not hammering) on transient server errors.
- **Human visual review**: automated filters catch mechanical problems (resolution, duplicates) but can't reliably judge "is this a clean, close-up, single-fish photo" — that judgment is made via a generated review page (`reports/review.html`), not by further heuristic tuning.
- **Citation follow-up (open)**: GBIF's own guidance recommends registering a "derived dataset" DOI for data pulled via the search API (as opposed to a bulk download) — this is required before this dataset is cited in any publication, and is tracked as an open action, not yet done.

Full detail, including the reasoning behind each of these choices, is in [CLAUDE.md](CLAUDE.md).

---

## Planned Approach

1. **Fish identification & extraction** (`src/fish_extractor/`, code-complete, run via [`notebooks/Phase1_Fish_Extraction.ipynb`](notebooks/Phase1_Fish_Extraction.ipynb) on Colab) — Grounded SAM 2 (Grounding DINO zero-shot text-prompted detection + SAM 2.1 segmentation) identifies whether an image shows exactly one, roughly-centered fish and, if so, extracts it as a mask-cutout crop. No training required; anything ambiguous is routed to a human-reviewable page rather than silently accepted or dropped.
2. **Pattern extraction** (`src/pattern_extractor/`, working prototype, run via [`notebooks/Phase2_Pattern_Extraction.ipynb`](notebooks/Phase2_Pattern_Extraction.ipynb)) — three independent feature extractors run on the fish-extractor's output: coloring (dominant-colour clustering), spots/freckles (connected-component blob shape), stripes (region elongation + per-channel FFT periodicity along the body axes). Kept separate rather than one blended feature vector, because these are developmentally distinct pattern-generating mechanisms (see [BACKGROUND.md](BACKGROUND.md)) — corroborated by the candidate-gene literature review (`data/genes/`), not just an engineering choice. The colour-clustering step is a Python reimplementation of *patternize*'s reference-initialized k-means method (Van Belleghem, S. M., Papa, R., Ortiz-Zuazaga, H., Hendrickx, F., Jiggins, C. D., McMillan, W. O., & Counterman, B. A. (2018, online 2017). patternize: An R package for quantifying colour pattern variation. *Methods in Ecology and Evolution*, 9(2), 390–398. https://doi.org/10.1111/2041-210X.12853), the closest published, peer-reviewed method for this task — demonstrated on fish and used for phylogenetic comparative colour-pattern studies. Two deliberate departures from the original, both because this study compares 64 different species rather than variation within one: no cross-specimen pixel registration (patternize's homology step assumes a broadly consistent body plan), and the stripe/spot geometry analysis is this project's own extension on top of patternize's colour-region output, not a capability patternize itself provides.
3. **Distance matrices** — three separate species x species visual-distance matrices (one per pattern dimension), plus one patristic-distance matrix from the restricted (genetically-sampled) molecular tree, for the secondary Mantel check described below. Building these first requires collapsing each species' per-image feature rows (`pattern_extractor`'s current output is one row per image, not per species — see `is_reference` note below) into one per-species vector per dimension; that aggregation rule is not yet specified and needs to cover the aggregation function itself (a plain arithmetic mean is wrong for hue, which is circular) plus a real, checked precision problem: cross-referencing `data/raw_images_state.json` against the 50-species Kmult set (`data/phylogeny/species_coverage.csv`) shows **4 of the 50** species are below the 25-image target — *Naso maculatus* (**1 image**), *Naso tuberosus* (13), *Zebrasoma gemmatum* (20), *Naso fageni* (22) — a worst-case 25x spread driven entirely by *N. maculatus*'s single image (mean-based spread across just those 4 is a milder 1.8x). The other 5 originally short-of-target species (*A. albimento*, *A. chronixis*, *Naso tergus*, *Prionurus chrysurus*, *A. grammoptilus*) are already excluded from the 50-species set by the phylogeny restriction, so they don't compound this. **Checked directly (`data/raw_images/Naso/Naso_maculatus.zip`): that single image *is* `000_reference.jpeg` — GBIF sourcing found zero acceptable field photos for this species, so its one image and the curated seed photo are the same file, not two separate things.** That collapses two supposedly-independent open decisions into one: excluding `is_reference` rows from the aggregate and retaining all 50 species in the Kmult analysis are **mutually exclusive** for this species specifically — including the reference photo gives *N. maculatus* an n=1 aggregate (already the sensitivity-check case above); excluding it drops the species entirely, taking the analysis set to 49. Whichever way `is_reference` handling is decided, it must be decided jointly with species retention for this case, not discovered as an `assert matched == 50` failure in unrelated tree-matching code. The aggregation step needs to record per-species image counts alongside the output, and each of the three sensitivity runs (drop *N. maculatus* only → n=49; drop all 4 sparse species → n=46; exclude `is_reference` rows, if that's the chosen policy → also n=49 for the same underlying reason) must assert its own expected species count rather than reusing the primary run's `assert matched == 50`.
4. **Statistical test** — **Kmult** (Adams, D. C. (2014). A generalized K statistic for estimating phylogenetic signal from shape and other high-dimensional multivariate data. *Systematic Biology*, 63(5), 685–697. https://doi.org/10.1093/sysbio/syu030) is the primary per-dimension test, run on each pattern dimension's per-species multivariate feature data against the restricted 50-species phylogeny (`data/phylogeny/`). This replaces an earlier plan to use Mantel tests with Harmon & Glor's (2010) phylogenetic-permutation remedy as primary — that remedy specifically targets inflated type-I error in *three-way* Mantel tests, and this project's per-dimension tests are two-way (pattern vs. phylogeny), which Harmon & Glor's own paper says is better served by a K-statistic (Kmult is its multivariate generalization) than by Mantel. **`geomorph::physignal.z()` (not `physignal()`) is the single function planned per dimension**, not two: checked directly against the `geomorph` reference manual, `physignal()`'s own documentation states that as of geomorph 4.0.2 it "no longer reports an effect size" and points users to `physignal.z` for that — and `physignal.z()` already returns everything `physignal()` does (the K statistic itself) plus the optimized branch-scaling parameter (lambda) and the standardized effect size (Z), computed together under one model rather than as two separate analyses that could disagree with no way to reconcile them. `compare.physignal.z` (Collyer, Baken & Adams (2022). A standardized effect size for evaluating and comparing the strength of phylogenetic signal. *Methods in Ecology and Evolution*, 13(2), 367–382. https://doi.org/10.1111/2041-210X.13749) then compares the three dimensions' `physignal.z` results directly — this is the tool for "which pattern dimension carries the most signal," a question three separate p-values can't answer since raw K values aren't on a comparable scale. **Lambda method, preregistered here rather than deferred: `lambda = "front"`, with one shared `PAC.no` value applied across all three pattern dimensions** (not chosen post hoc, and not a per-dimension value, even though the three feature matrices differ in width). This matches the `geomorph` manual's own `compare.physignal.z` worked example exactly — it calls `physignal.z(..., lambda = "front", PAC.no = 7)` identically for both traits being compared, before comparing them — the package authors' demonstrated usage for precisely this "compare signal across trait subsets on one tree" case. `"front"` was chosen over the other three documented options for a specific reason tied to this project's history: `"burn"` explicitly "sample[s] a spectrum of lambda from 0 to 1... to find the lambda value that maximizes the effect size rather than the log-likelihood" — but Z, the effect size, is exactly what `compare.physignal.z` compares across the three dimensions, so using `"burn"` would mean comparing three separately-maximized quantities, a shape of risk this project has already been burned by once (the pre-rebuild pipeline's data-dredged phylogenetic-signal result). `"all"` "will tend to optimize lambda at 0 or 1," which is precisely the documented `NaN`-Z failure case already guarded against above, so it carries the highest exposure to that failure mode. `"front"`'s own documented cost: it "will tend to bias lambda toward 1, making analysis more similar to using multivariate K, as in physignal" — a real limitation to carry forward, not a free choice. `PAC.no` itself will be chosen from the most data-constrained of the three feature matrices and reported (per-dimension component counts recorded alongside), since `PAC.no` sets the dimensionality that Z's underlying log-likelihood is estimated in, and comparing Z values estimated at different dimensionalities would need its own justification the manual doesn't provide. **Seed handling**: `physignal.z`'s `seed` argument defaults to `NULL`, which the manual states gives "the exact same P-values... for repeated runs" — that deterministic default will be used (not `seed = "random"`), consistent with this project's other reproducibility measures (the pinned tree checksum, the saved synonym table). (`physignal()` at full, untransformed-tree dimensionality may still be run as an optional, explicitly-labeled unit-tree sensitivity check alongside the primary `physignal.z` result, not as a second primary result.) The three pairwise-distance Mantel tests (one per pattern dimension, phylogenetically-permuted) are retained as a **secondary**, distance-based check. A multiple-comparisons correction across the three dimensions applies to both the primary `physignal.z` results and the secondary Mantel results before anything is called significant. Under either test, a null result is reported as inconclusive, not as evidence of no association — Harmon & Glor found the Mantel test has real power limitations even with the correct remedy applied.
   - **Why not `physignal.eigen`**: also part of `geomorph` (Mitteroecker, Collyer & Adams 2024), it decomposes *where within one feature matrix* phylogenetic signal concentrates (which dimensions of that matrix carry it) — a different question from this project's actual one, which is comparing signal strength *across* three separate feature matrices (color vs. stripe vs. spot). `compare.physignal.z` is the right tool for that comparison; `physignal.eigen` was considered and isn't a substitute for it, though it could be a useful follow-up if a single dimension turns out to carry most of the signal and *which features within it* becomes the next question.
   - **Data-admissibility note, not just implementation detail**: every `physignal`/`physignal.z` argument in the `geomorph` manual is documented in terms of "Procrustes shape variables" from Generalized Procrustes Analysis — the package's default assumption is shape data, and Adams (2014)'s title phrase ("shape and *other* high-dimensional multivariate data") is the sole basis for using it on this project's non-shape colour/stripe/spot features. That makes the log-ratio transform, standardization, and condition-number check below **admissibility requirements for using a shape-signal method on non-shape data**, not routine data hygiene — skipping them isn't just sloppy, it's applying the method outside where its own documentation says it's been shown to work.
   - **Implementation language, revised**: `physignal.z`/`compare.physignal.z` are not simple formulas to port — per the `geomorph` manual they involve phylogenetically-aligned component projection, likelihood optimization of the branch-length scaling parameter, RRPP-based permutation, and a Box-Cox standardized effect size, and this project has already shipped one silently-wrong metric (the pre-rebuild pipeline's misreported ROC-AUC). An "almost right" hand port returns a plausible, wrong number with no visible error. Preferred approach: run these specific statistics in R (via `rpy2` or as a documented standalone R step Python hands the pruned tree and feature matrices to) rather than reimplementing them — the feature matrices are small (50 x p), so there's no performance case for a Python port here the way there was for `pattern_extractor`'s per-pixel clustering. If a Python port is done anyway, it is gated on a numerical-equivalence test: run `physignal.z`/`compare.physignal.z` on `geomorph`'s own `plethspecies` example dataset (confirmed present in the package, used in the manual's own examples for both functions) in R, commit the output, and require the port to reproduce it to a stated tolerance as a unit test before it's trusted on real data — the same gap applies to the *patternize* colour-clustering reimplementation already in `pattern_extractor` (no equivalence check against the R package exists yet), tracked as a follow-up, not blocking Phase 2's existing prototype status.
   - **Feature-matrix requirements for Kmult**, to satisfy before running (binding fast at n=50, not yet built): compositional features (e.g. colour proportions summing to 1) need a log-ratio transform (CLR/ILR) since raw proportions make the covariance matrix singular; mixed-unit features need standardization; dimensionality must stay well under n=50 with the reduction choice reported, not defaulted silently; a rank/condition-number check on each dimension's feature matrix must fail loudly on ill-conditioned input rather than silently returning a number. Watch for `physignal.z` returning `NaN` when the phylogenetic scaling parameter optimizes to 0 — the `geomorph` manual itself says the standard error is also 0 in that case and pairwise comparisons "might not make sense."
   - **Tree and matching integrity checks**, not yet built: assert the loaded tree's tip count falls in a range that discriminates the genetic-data-only tree from the much larger "complete" tree (e.g. `10,000 < ntip < 15,000`, with both candidate counts — 11,638 genetic-data tips vs. 31,526 complete-tree tips, both from Rabosky et al. 2018 — named in a comment) rather than an exact `== 11638` check, which would break on any release that differs by even one tip; prune to the 50 study species *before* computing any patristic-distance/cophenetic matrix, not after (the unpruned matrix is ~1.08 GB as float64 vs. ~20 KB pruned); and assert that feature-matrix row order matches the pruned tree's tip-label order, failing loudly on mismatch — `geomorph` matches by name, and a Python port will match by array position unless written not to, which would produce a well-formed, meaningless result on any accidental reordering.
   - Species-count integrity: `assert matched == 50` (or equivalent) when loading `species_coverage.csv`, so a drift in the matched count is a loud failure, not a silently stale number.
5. **(Contingent, deferred)** If any pattern dimension shows a significant, corrected result: investigate whether the 56 candidate genes identified in the literature review (`data/genes/`) are present/annotated in the genome assemblies available for 16 of the 64 species (`data/genome_assemblies/`), and whether any expression data can be found. Not yet scoped in detail.

Implementation details (exact feature set, split strategy for validating pattern extraction against manually labeled ground truth) are being finalized as each stage is rebuilt.

---

## Repository Structure

```
Surgeonfish_Neural_Network_Phylogenetics/
├── data/
│   ├── raw_images/                    # One subfolder per species, one per genus
│   │   ├── Acanthurus/
│   │   │   └── Acanthurus_guttatus/
│   │   │       ├── 000_reference.jpg  # Pre-existing curated photo (seed image)
│   │   │       └── 001_gbif_....jpg   # GBIF-sourced, license-checked, reviewed
│   │   ├── Ctenochaetus/
│   │   ├── Naso/
│   │   ├── Paracanthurus/
│   │   ├── Prionurus/
│   │   └── Zebrasoma/
│   ├── genus_species_counts.json      # NCBI per-genus binomial-species denominators (Species section)
│   ├── genes/                         # Candidate-gene lists from the literature review
│   │   ├── genes_coloring.txt
│   │   ├── genes_stripes.txt
│   │   └── genes_spots.txt
│   ├── genome_assemblies/             # NCBI assembly-availability search (metadata only)
│   │   ├── manifest.csv
│   │   └── README.md
│   └── phylogeny/                     # Fish Tree of Life genetic-data-only tree + coverage
│       ├── actinopt_12k_treePL.tre
│       ├── species_coverage.csv
│       ├── acanthuridae_synonyms.json
│       └── README.md
├── reports/
│   ├── image_sourcing_log.csv         # Per-image source URL, license, attribution
│   └── review.html                    # Human visual-review page (keep/reject)
├── notebooks/                          # Colab notebooks for the GPU/long-running stages
│   ├── Phase1_Fish_Extraction.ipynb    # Runs fish_extractor against data/raw_images/
│   └── Phase2_Pattern_Extraction.ipynb # Runs pattern_extractor against Phase 1's output
├── src/
│   ├── dataset_builder/               # GBIF sourcing pipeline (see its docstring)
│   ├── fish_extractor/                # Fish identification & extraction (code-complete)
│   └── pattern_extractor/             # Coloring/stripe/spot feature extraction (prototype)
├── tests/
│   ├── dataset_builder/
│   ├── fish_extractor/
│   └── pattern_extractor/
├── BACKGROUND.md                      # Candidate-gene / pattern-genetics literature review
├── LICENSE
└── README.md
```

Everything else (distance matrices, phylogenetic analysis, and the scripts that produce them) will be added back stage by stage.

---

## Acknowledgements

This project is conducted in the **Dornburg Lab** at the University of North Carolina Charlotte. Phylogenetic reference data are obtained from the Fish Tree of Life (fishtreeoflife.org). GPU computation (fish detection/segmentation) runs on Google Colab; the Phase 2 pattern-extraction methodology is a Python reimplementation of *patternize* (Van Belleghem et al. 2018) — see Planned Approach for the full citation.
